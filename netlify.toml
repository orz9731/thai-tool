# Netlify configuration for thai-tool

[build]
  command = "echo 'Static site - no build required'"
  publish = "."

# Edge Functions routing
[[edge_functions]]
  path = "/api/tts"
  function = "tts"
